{
  "name": "simple-client",
  "version": "0.1.0",
  "description": "having fun",
  "scripts": {
    "start": "react-scripts start",
    "eject": "react-scripts eject",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "----- // CLEANING SCRIPTS // -----": "",
    "clean:npm": "rm -rf ./node_modules",
    "clean": "rm -rf node_build && rm -rf build",
    "reset": "npm run clean && npm run clean:npm",
    "----- // CLEANING CORDOVA // -----": "",
    "build:cordova": "yarn build && mkdir -p ./cordova/www && rm -rf ./cordova/www && mv build ./cordova/www"
  },
  "dependencies": {
    "react-scripts": "3.3.1",
    "@subnik/react-redux-client": "^0.1.3",
    "@forrestjs/feature-network": "^3.9.0",
    "@forrestjs/feature-storage": "^3.9.0"
  },
  "/------- CORDOVA related ------ /": "",
  "__homepage": "./",
  "/------------------------------ /": "",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
